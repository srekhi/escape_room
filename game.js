class Game {
  constructor(){
      //create document event listener
      //if key press is w,a,s,d do something,
      //point.move(right);
      //if space fire off rays,
      //if w
      window.addEventListener("keypress", ()=>{

      });
  }

  animate(){
    requestAnimationFrame(this.animate);
    
  }


}
